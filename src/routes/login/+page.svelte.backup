<script lang="ts">
	import { base } from "$app/paths";
	import { page } from "$app/state";
	import { usePublicConfig } from "$lib/utils/PublicConfig.svelte";
	import Logo from "$lib/components/icons/Logo.svelte";
	import { onMount } from "svelte";
	import { browser } from "$app/environment";

	const publicConfig = usePublicConfig();

	function handleLogin() {
		console.log("🔵 Login button clicked!");
		console.log("🔵 Current URL:", window.location.href);
		console.log("🔵 Page URL:", page.url.toString());
		
		const next = page.url.searchParams.get("next") || base || "/";
		console.log("🔵 Next param:", next);
		
		// Make a direct navigation to trigger server-side handler
		// This will cause a full page reload and hit the +server.ts endpoint
		const targetUrl = `${window.location.origin}${base}/login?next=${encodeURIComponent(next)}`;
		console.log("🔵 Navigating to:", targetUrl);
		
		window.location.href = targetUrl;
	}
</script>

<svelte:head>
	<title>Sign In - {publicConfig.PUBLIC_APP_NAME}</title>
</svelte:head>

<div class="flex min-h-screen items-center justify-center bg-gradient-to-br from-gray-50 to-gray-100 dark:from-gray-900 dark:to-gray-800 px-4">
	<div class="w-full max-w-md">
		<div class="bg-white dark:bg-gray-800 rounded-2xl shadow-xl p-8 space-y-8">
			<!-- Logo and Title -->
			<div class="text-center space-y-4">
				<div class="flex justify-center">
					<Logo classNames="size-16 dark:invert" />
				</div>
				<h1 class="text-3xl font-bold text-gray-900 dark:text-gray-100">
					Welcome to {publicConfig.PUBLIC_APP_NAME}
				</h1>
				<p class="text-gray-600 dark:text-gray-400">
					Sign in to start chatting with AI models and generate images
				</p>
			</div>

			<!-- Sign In Button - Official Google Branding -->
			<button
				type="button"
				onclick={handleLogin}
				class="w-full transition-all duration-200 hover:shadow-lg active:scale-[0.98]"
			>
				<img
					src="/google-signin-light.svg"
					alt="Sign in with Google"
					class="w-full h-auto dark:hidden"
				/>
				<img
					src="/google-signin-dark.svg"
					alt="Sign in with Google"
					class="w-full h-auto hidden dark:block"
				/>
			</button>

			<!-- Features List -->
			<div class="pt-6 space-y-3 border-t border-gray-200 dark:border-gray-700">
				<p class="text-sm text-gray-600 dark:text-gray-400 font-medium">What you can do:</p>
				<ul class="space-y-2 text-sm text-gray-600 dark:text-gray-400">
					<li class="flex items-start gap-2">
							<svg
								class="size-5 text-green-500 flex-shrink-0 mt-0.5"
								fill="none"
								stroke="currentColor"
								viewBox="0 0 24 24"
							>
								<path
									stroke-linecap="round"
									stroke-linejoin="round"
									stroke-width="2"
									d="M5 13l4 4L19 7"
								/>
							</svg>
							<span>Chat with state-of-the-art AI models</span>
						</li>
					<li class="flex items-start gap-2">
							<svg
								class="size-5 text-green-500 flex-shrink-0 mt-0.5"
								fill="none"
								stroke="currentColor"
								viewBox="0 0 24 24"
							>
								<path
									stroke-linecap="round"
									stroke-linejoin="round"
									stroke-width="2"
									d="M5 13l4 4L19 7"
								/>
							</svg>
							<span>Generate stunning images with FLUX AI</span>
						</li>
					<li class="flex items-start gap-2">
							<svg
								class="size-5 text-green-500 flex-shrink-0 mt-0.5"
								fill="none"
								stroke="currentColor"
								viewBox="0 0 24 24"
							>
								<path
									stroke-linecap="round"
									stroke-linejoin="round"
									stroke-width="2"
									d="M5 13l4 4L19 7"
								/>
							</svg>
							<span>Save and manage your generated images</span>
						</li>
					<li class="flex items-start gap-2">
							<svg
								class="size-5 text-green-500 flex-shrink-0 mt-0.5"
								fill="none"
								stroke="currentColor"
								viewBox="0 0 24 24"
							>
								<path
									stroke-linecap="round"
									stroke-linejoin="round"
									stroke-width="2"
									d="M5 13l4 4L19 7"
								/>
							</svg>
							<span>Access your conversation history</span>
						</li>
				</ul>
			</div>

			<!-- Privacy Notice -->
			<p class="text-xs text-center text-gray-500 dark:text-gray-400">
				By signing in, you agree to our Terms of Service and Privacy Policy
			</p>
		</div>
	</div>
</div>
